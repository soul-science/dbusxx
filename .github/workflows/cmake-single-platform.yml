# This starter workflow is for a CMake project running on a single platform. There is a different starter workflow if you need cross-platform coverage.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-multi-platform.yml
name: CMake on a single platform

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
  BUILD_TYPE: Release

jobs:
  build:
    # The CMake configure and build commands are platform agnostic and should work equally well on Windows or Mac.
    # You can convert this to a matrix build if you need cross-platform coverage.
    # See: https://docs.github.com/en/free-pro-team@latest/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install dependencies on Ubuntu
      run: |
        sudo apt-get update
        sudo apt-get install -y g++ pkg-config libsystemd-dev

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: cmake -B ${{github.workspace}}/build-release -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}

    - name: Build
      # Build your program with the given configuration
      run: cmake --build ${{github.workspace}}/build-release --config ${{env.BUILD_TYPE}}

    - name: Test
      working-directory: ${{github.workspace}}/build-release
      # Execute tests defined by the CMake configuration.
      # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
      run: ctest -C ${{env.BUILD_TYPE}}

    - name: Package artifacts
      run: |
        # 获取版本号（从 tag 获取，如 v1.0.0）
        VERSION=${GITHUB_REF_NAME}
        # 压缩 build 目录下的文件，并重命名为 项目名-版本号-linux.tar.gz
        tar -czvf ${{github.workspace}}/dbusxx-${VERSION}-ubuntu.tar.gz -C ${{github.workspace}}/build-release .
        # 顺便生成校验和
        sha256sum ${{github.workspace}}/dbusxx-${VERSION}-ubuntu.tar.gz > ${{github.workspace}}/dbusxx-${VERSION}-ubuntu.tar.gz.sha256

    - name: Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
            ${{github.workspace}}/dbusxx-*.tar.gz
            ${{github.workspace}}/dbusxx-*.tar.gz.sha256
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
